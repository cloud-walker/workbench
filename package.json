{
  "private": true,
  "scripts": {
    "types": "turbo run types",
    "coverage": "turbo run coverage",
    "build": "turbo run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.4.3"
  }
}
