{
  "private": true,
  "scripts": {
    "types": "turbo run types",
    "test": "turbo run test",
    "coverage": "turbo run coverage",
    "build": "turbo run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.24.3",
    "turbo": "^1.4.3"
  }
}
